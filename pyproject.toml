[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "base120"
version = "1.0.0"
description = "Base120 v1.0.0 Reference Implementation"
requires-python = ">=3.10"
license = { text = "MIT" }

[project.scripts]
base120 = "base120.cli.main:main"

