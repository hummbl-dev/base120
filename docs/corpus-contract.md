# Base120 Golden Corpus Contract

The files in `tests/corpus` define the authoritative behavior of Base120. Any implementation is correct **if and only if** it produces byte-for-byte identical error outputs for all corpus inputs.
